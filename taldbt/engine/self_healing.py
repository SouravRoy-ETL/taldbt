# DEPRECATED: Self-healing was scrapped.
# Changing SQL logic (INNER JOIN → LEFT JOIN) in production would corrupt business semantics.
# Use validation.py for diagnostics instead — diagnose everything, fix nothing.
